
import { NativeModules, Platform } from 'react-native';

export default function RNFireworkTvLib() {
    const openFirework = () => {
        // if (Platform.OS === "android") {
        //     var FireworkSDK = NativeModules.RNFireworkTvLib;
        // } else {
        //     var FireworkSDK = NativeModules.FireworkIOS;
        // }
        // FireworkSDK.open();
    }
    openFirework();
}
